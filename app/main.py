"""
Archived module: legacy FastAPI entrypoint.

Active app is `app/server.py`. Start with:
  uvicorn app.server:app --reload

Version: 1.0.2 - Workflow conflict fixed, ready for CI/CD
"""

raise SystemExit("This module is archived. Use `uvicorn app.server:app`. ")
