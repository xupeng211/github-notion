"""
Archived module: early sqlite3 helper. The project has migrated to SQLAlchemy
in `app/models.py`. This stub exists to prevent accidental imports from
breaking the runtime. Remove after downstream references are updated.
"""

raise SystemExit("This module is archived. Use SQLAlchemy models in app.models.")
